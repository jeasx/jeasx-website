:3000 {
    root * www
    file_server

    @assets {
        path *.*
    }

    header @assets {
        Cache-Control "public, max-age=31536000, immutable"
    }

    handle_errors {
        rewrite * /{err.status_code}.html
        file_server
    }
}